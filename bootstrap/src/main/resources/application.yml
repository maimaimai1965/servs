server:
  shutdown: graceful
spring:
  mvc:
    converters:
      preferred-json-mapper: gson
  main:
    allow-bean-definition-overriding: true
    banner-mode: off
  lifecycle.timeout-per-shutdown-phase: 9s
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/trt?autoReconnect=true&maxReconnects=2&connectTimeout=100&serverTimezone=Europe/Kiev
    username: root
    password: root
  jooq:
    sql-dialect: MYSQL
jwt:
  secret: MIICCgKCAgEAu9r5sLpyNSH618apltlGeZ59pYmdUKScA8h6MiVT2dU9g/IVdv7d
  type: JWT
  issuer: Mock
  audience: Test
  token-type: bearer
  expires-in: 43199
  scope: openid
  tenant: XM
feign:
  httpclient:
    disable-ssl-validation: true
  okhttp:
    enabled: true
  client:
    config:
      default:
        connect-timeout: 5000
        read-timeout: 30000
        logger-level: basic
servs:
  modules:
    aaa:
      host: localhost
      port: 5001
      endpoints:
        operations:
          method001-servA01: /servs/api/servs001/v1/method001ServA01
          method002-servA01: /servs/api/servs001/v1/method002ServA01
      operation-method-uri:
    bbb:
      host: localhost
      port: 5002
